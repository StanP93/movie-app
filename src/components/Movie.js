import React from 'react'
import classes from './Movie.module.css'

const Movie = (props) => {
    return (
        <div className={classes.myMovie}>
            <img/>
        </div>
    )
}

export default Movie;
